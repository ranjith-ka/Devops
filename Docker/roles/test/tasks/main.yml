# tasks file for test
- name: pull an image
  docker_image:
    name: ranjithka/ansible-rk:1.01

- name: Create a data container
  docker_container:
    name: mydata
    image: busybox
    volumes:
      - /data
