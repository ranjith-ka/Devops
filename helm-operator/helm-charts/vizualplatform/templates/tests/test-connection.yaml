apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vizualplatform.fullname" . }}-test-connection"
  labels:
    {{- include "vizualplatform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vizualplatform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
