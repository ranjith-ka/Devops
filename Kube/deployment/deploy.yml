apiVersion: v1
kind: Pod
metadata:
  name: jenkins
  labels:
    name: jenkins
spec:
  containers:
  - name: jenkins
    image: ranjithka/jenkins:latest
    ports:
      - name: jenkins-port
        containerPort: 8080 
  restartPolicy: OnFailure  