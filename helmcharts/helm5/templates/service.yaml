apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm5.fullname" . }}
  labels:
    {{- include "helm5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm5.selectorLabels" . | nindent 4 }}
