# permissions for end users to edit helm3s.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: helm3-editor-role
rules:
- apiGroups:
  - charts.vizualplatform.com
  resources:
  - helm3s
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - charts.vizualplatform.com
  resources:
  - helm3s/status
  verbs:
  - get
