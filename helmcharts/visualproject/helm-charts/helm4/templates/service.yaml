apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm4.fullname" . }}
  labels:
    {{- include "helm4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm4.selectorLabels" . | nindent 4 }}
